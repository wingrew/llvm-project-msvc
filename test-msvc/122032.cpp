/*
clang 拒绝此代码，因为定义没有提供固定的底层类型，但 MSVC 和 edg w/--microsoft接受它作为扩展
错误的核心问题：

在 C++ 中，枚举可以分为两种：非作用域枚举（enum）和作用域枚举（enum class 或 enum struct）。C++11 引入了固定底层类型的枚举（通过 : 类型 指定，例如 : int）。
如果一个枚举被前向声明为具有固定底层类型（如 enum E : int;），那么后续的定义（enum E {}）必须：
使用相同的底层类型（这里是 int）。
保持相同的枚举种类（作用域或非作用域）。
不能省略底层类型（即不能在定义时不指定底层类型）。
在代码中，enum E {}; 没有指定底层类型，而之前的声明 enum E : int; 指定了底层类型为 int，这导致了冲突。

*/

enum E : int;
enum E {};

int main() {
	return 0;
}